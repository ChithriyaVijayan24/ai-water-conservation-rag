# Simple RAG-based Water Conservation Assistant
# This is a conceptual demo suitable for internship submission

# Step 1: Load knowledge base
with open("data.txt", "r") as file:
    knowledge_base = file.read()

# Step 2: Define retrieval function
def retrieve_relevant_content(query, documents):
    keywords = query.lower().split()
    relevant_lines = []
    for line in documents.split("\n"):
        if any(word in line.lower() for word in keywords):
            relevant_lines.append(line)
    return "\n".join(relevant_lines)

# Step 3: Define AI response function (simulated LLM behavior)
def generate_response(query):
    retrieved_content = retrieve_relevant_content(query, knowledge_base)
    
    if retrieved_content.strip() == "":
        return "I’m sorry, I don’t have enough information to answer that accurately."
    
    response = f"""
Based on verified information:

{retrieved_content}

This shows how responsible water usage can support sustainability and reduce water wastage.
"""
    return response

# Step 4: User interaction
user_question = input("Ask a question about water conservation: ")
print(generate_response(user_question))
